/*
 * インデントとフォーマット
 * C言語の最小単位は「トークン」
 * 下記のコードではint や main 、 return など。
 * トークンは半角スペースで区切られなくてはならない
 * 区切られてさえいればよい
 * なお、1文字でトークンとして成り立つ記号 ( ) とか ;　は、
 * スペースで区切られななくても良い。
 * 1文字のトークンとして、隣り合う文字と区別されるから。
 */

int main(void)
{
    return 0;
}